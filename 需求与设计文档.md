# ExcelToWord工具 需求与设计文档

## 文档信息
- **版本号**：1.1
- **维护日期**：2026-04-30

---

## 1. 软件概述

### 1.1 软件名称与版本
| 项目 | 内容 |
|------|------|
| 软件名称 | ETW (Excel To Word) |
| 当前版本 | 1.1 |
| 版本格式 | X.X（两位版本号） |
| 版本管理规则 | 主版本号.次版本号 |

### 1.2 版本管理规范
- **主版本号（X）**：当软件有重大架构调整或功能变更时递增
- **次版本号（Y）**：当软件有功能新增或bug修复时递增
- **初始版本**：1.0
- **版本示例**：1.0 → 1.1 → 2.0 → 2.1

### 1.3 版本历史
- **1.0** (2026-04-30) - 初始版本
- **1.1** (2026-04-30) - 新增/追加/覆盖模式逻辑优化：
  - 输出文件不存在时创建新文档
  - 输出文件存在时支持追加和覆盖两种模式
  - 新增表格自动添加4级标题（用例编号:用例标题）

### 1.4 项目目标
开发一个命令行工具，将Excel中的数据转换并填入Word文档的表格中，支持新增、追加和覆盖三种工作模式。

### 1.5 适用范围
适用于测试报告、问题跟踪表等需要从Excel批量导入数据到Word模板的场景。

---

## 2. 功能需求

### 2.1 核心功能

#### 2.1.1 Excel转Word
- 读取Excel文件中的数据
- 将每行数据按照列名创建Word表格
- 每个Excel行生成一个带4级标题的表格

#### 2.1.2 工作模式（根据输出文件状态自动判断）

| 输出文件状态 | 参数条件 | 工作模式 | 说明 |
|-------------|---------|---------|------|
| 不存在 | 任意 | **新增模式** | 创建新文档，每个Excel行一个带标题的表格 |
| 存在 | 无CK参数 | **追加模式** | 在原文档末尾追加新的表格 |
| 存在 | 有CK参数 | **覆盖模式** | 根据CK匹配更新已有表格的值，未匹配则新增 |

### 2.2 数据结构定义

#### 2.2.1 Excel数据结构
- 每个Excel文件包含一个数据表
- 第一行为列标题行
- 后续各行为数据行

#### 2.2.2 Word文档结构
- Word文档包含多个表格
- 每个表格结构：2列（字段名 | 值）
- **行名定义**：每个表格的第一列值称为"行名"
- **表格标题**：4级标题，格式为 `{用例编号}:{用例标题}`

#### 2.2.3 预定义行名排序
Word表格的行名按以下顺序排列：
```
试验标识 → 用例编号 → 用例标题 → 问题等级 → 问题标题 → 问题描述 → 
原因分析 → 整改措施 → 整改方 → 回归步骤 → 回归人员 → 问题单编号 → 
问题单类型 → 整改方联系人 → 回归单编号 → 回归单类型
```

---

## 3. 命令行接口

### 3.1 语法格式
```
etw.py -i <输入Excel> -o <输出Word> [-F] [--CK1:value1] [--CK2:value2] ... [--HM1:name1] [--HM2:name2] ...
```

### 3.2 参数说明

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `-i` | 文件路径 | 是 | 指定输入的Excel文件路径 |
| `-o` | 文件路径 | 是 | 指定输出的Word文件路径 |
| `-F` | 标志位 | 否 | 强制覆盖模式：未匹配的Excel行不新增表格 |
| `--CK1` ~ `--CK4` | 字符串 | 否 | 根字段参数，用于匹配Excel列名与Word行名。最多4个，多个时为AND关系 |
| `--HM1` ~ `--HM20` | 字符串 | 否 | 覆盖字段参数，指定需要覆盖的列名/行名。最多20个，不指定则覆盖所有字段 |

### 3.3 使用示例

#### 示例1：首次转换（输出文件不存在）
```bash
etw.py -i source.xlsx -o dest.docx
```
创建新Word文档，每个Excel行生成一个带4级标题的表格。

#### 示例2：追加数据（输出文件已存在，无CK参数）
```bash
etw.py -i source2.xlsx -o dest.docx
```
在原文档末尾追加新的表格。

#### 示例3：覆盖模式 - 根据CK匹配更新
```bash
etw.py -i source.xlsx -o dest.docx --CK1 用例编号
```
根据"用例编号"匹配Word中的表格，更新匹配表格的值，未匹配的行新增表格。

#### 示例4：强制覆盖模式
```bash
etw.py -i source.xlsx -o dest.docx -F --CK1 用例编号 --HM1 整改措施 --HM2 回归结果
```
只更新匹配的表格，未匹配的行不新增。指定只覆盖"整改措施"和"回归结果"两个字段。

---

## 4. 业务逻辑

### 4.1 模式判断规则

```
开始
  ↓
输出文件存在？
  ↓yes     ↓no
  ↓         ↓
有CK参数？   新增模式
  ↓yes     ↓no   （创建新文档）
  ↓         ↓
覆盖模式     追加模式
  ↓         ↓
结束
```

### 4.2 覆盖模式匹配逻辑

#### 4.2.1 CK参数匹配
- 多个CK参数之间为AND关系：必须同时满足所有条件才视为匹配
- 例如：设置`--CK1 用例编号 --CK2 试验标识`时，Excel行必须同时匹配这两个字段

#### 4.2.2 HM参数处理
- 使用HM参数指定需要覆盖的列名
- 不指定HM参数时，默认覆盖所有字段
- 指定的HM列名必须存在于预定义行名列表中

### 4.3 表格标题生成规则
- 格式：`{用例编号}:{用例标题}`
- 例如：`CSYL-WDAQ-001:验证集成测试发现的安全性风险项`
- 若用例编号和用例标题都为空，则显示"未命名"

### 4.4 处理流程（覆盖模式）
```
1. 解析命令行参数
2. 读取Excel文件
3. 加载现有Word文档
4. 遍历Excel每一行：
   a. 遍历Word中所有表格
   b. 根据CK参数查找匹配表格（AND关系）
   c. 找到匹配：
      - 按HM参数更新字段值
      - 未指定HM则更新所有字段
   d. 未找到匹配：
      - 非强制模式(-F)：在末尾新增表格
      - 强制模式：跳过
5. 保存Word文档
```

---

## 5. 技术方案

### 5.1 开发环境
- **Python版本**：3.10
- **操作系统**：Windows

### 5.2 依赖库
- `openpyxl` - Excel文件读取
- `python-docx` - Word文档操作
- `argparse` - 命令行参数解析（Python内置）

### 5.3 项目结构
```
ETW/
├── etw.py                 # 主程序入口（版本1.1）
├── requirements.txt       # 依赖库清单
├── README.md              # 使用说明文档
├── 需求与设计文档.md       # 本文档
└── 01etw需求.txt          # 原始需求文档（保留）
```

---

## 6. 验收标准

### 6.1 功能验收
- [x] 命令行参数正确解析
- [x] 输出文件不存在时创建新文档（新增模式）
- [x] 输出文件存在且无CK参数时追加表格（追加模式）
- [x] 输出文件存在且有CK参数时覆盖匹配表格（覆盖模式）
- [x] CK参数AND匹配正确
- [x] HM参数正确覆盖指定字段
- [x] 表格自动生成4级标题（用例编号:用例标题）
- [x] 非强制模式下未匹配行新增表格

### 6.2 性能要求
- 单次转换操作在10秒内完成（数据量<1000行）
- 内存占用不超过500MB

### 6.3 错误处理
- 输入文件不存在时报错提示
- Excel格式错误时报错提示
- Word文件只读时报错提示
- 覆盖模式未指定CK参数时报错提示